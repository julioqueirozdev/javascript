function inicio (){
    var num = window.prompt("Digite um número inteiro qualquer")
    var i = Number(num)
    window.alert(`Antes do númeoro ${i} temos o número ${i-1}.
    Depois do número ${i} temos o número ${i+1}.`)
}